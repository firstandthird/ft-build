options:
  livereload: true
lib:
  files:
    - <%= jshint.lib %>
  tasks:
    - lib
test:
  files:
    - <%= jshint.test %>
    - test/index.html
  tasks:
    - test
example:
  files:
    - example/*
tools:
  files:
    - <%= jshint.tools %>
  tasks: default
